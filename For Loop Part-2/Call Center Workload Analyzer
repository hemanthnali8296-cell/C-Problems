#include<stdio.h>
int main(){
   int n,calls,total=0;
   int count=0;
   scanf("%d",&n);
   for(int i=0;i<n;i++){
      scanf("%d",&calls);
      total+=calls;
      if(calls>40){
         count++;
      }
   }
   printf("Total Calls: %d\n",total);
   printf("Overloaded Hours: %d",count);
}
