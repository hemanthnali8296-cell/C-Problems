#include<stdio.h>
int main(){
int days,patients,sum=0,count=0;
scanf("%d",&days);
for(int i=0;i<days;i++){
scanf("%d",&patients);
sum+=patients;
if(patients>100){
     count++;
}
}printf("Total Patients: %d\n",sum);
 printf("OverCrowded days:%d",count);
return 0;
}
