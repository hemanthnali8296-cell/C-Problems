#include <stdio.h>
int main() {
  int a,b,c;
  scanf("%d %d %d",&a,&b,&c);
  
  if(a==b&&b==c){
    printf("Equalateral Triangle");
  }else if(a==b&&b!=c){
    printf("Isoscalane Triangle");
  }else {
    printf("Scalane Triangle");
  }
    return 0;
}
