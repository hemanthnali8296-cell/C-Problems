#include <stdio.h>
int main() {
    int  type,litres,cost=0;
    scanf("%d %d",&type,&litres);
    switch(type){
        case 1:
        cost=litres*105;
        printf("Fuel Cost ₹ %d",cost);
        break;
        case 2:
        cost=litres*92;
        printf("Fuel Cost ₹ %d",cost);
        break;
        case 3:
        cost=litres*85;
        printf("Fuel Cost ₹ %d",cost);
        break;
    }
}
